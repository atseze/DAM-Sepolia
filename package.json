{
  "name": "dam-alchemy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npx hardhat compile",
    "deploy-testnet": "npx hardhat run ./scripts/MyDAM_deploy.js --network ETH_GOERLI",
    "deploy": "npx hardhat run ./scripts/MyDAM_deploy.js --network ETH_MAINNET",
    "node": "npx hardhat node",
    "deploy-local": "npx hardhat run ./scripts/MyDAM_deploy.js --network localhost"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.3",
    "dotenv": "^16.0.2"
  },
  "devDependencies": {
    "@metamask/detect-provider": "^2.0.0",
    "@metamask/sdk": "^0.5.6",
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.17.0"
  }
}
